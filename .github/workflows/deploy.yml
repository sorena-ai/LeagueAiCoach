name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Rebuild sensei-api on server
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          command_timeout: 30m
          script: |
            set -e
            cd ~/workspace/infra
            git submodule update --remote LeagueAiCoach
            rm -rf LeagueAiCoach/data
            git clone --depth 1 https://github.com/sorena-ai/awesome-league-of-legends.git LeagueAiCoach/data
            docker compose up -d --build sensei-api
